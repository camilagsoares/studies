let age: number = 5;
const firstName: string = "Camila";
const isValid: boolean = true;
let idk: any = 5;

//A lista(array) de nomes deve ser somente do tipo String
const listaNomes: string[] = ["Camila", "Lu"];


// Tupla - Ordem exata dos valores que a gente quer
const person: [number,string] = [3,'Camila']

// Lista de tuplas que vai ser uma lista
const people: [number,string][] = [
    [1,'Jane'],
    [2,'Doe']
]

// Intersections - ser uma coisa ou outra
const productId: string | number | boolean = 1

// Enum


console.log(age);
